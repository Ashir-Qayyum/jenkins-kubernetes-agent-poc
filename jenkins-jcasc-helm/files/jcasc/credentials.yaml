credentials:

  system:
    domainCredentials:
      - credentials:

          - usernamePassword:
              scope: GLOBAL
              id: dockerhub-creds                       # THis id will be used for reference in the Jenkinsfile
              description: Docker Hub Credentials       # I set it cause later when i write the pipeline,
              username: ashirqayyum                     # it will pull/push images into dockerhub registry
#              password: <my dockerhub PAT>     
